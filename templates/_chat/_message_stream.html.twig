<turbo-stream action="append" target="messages_list-{{ chat_id }}">
    <template id="message-{{message.id}}">
        {% include '_chat/_message.html.twig' with {message: message} %}
    </template>
</turbo-stream>